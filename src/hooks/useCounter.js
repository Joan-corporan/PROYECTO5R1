/* import { useState } from "react";

export const useCounter = (inicialValor = 0) => {
  const [counter, setCounter] = useState(inicialValor);
  const increment = () => {
    setCounter(counter + 1);
  };
  const descrement = () => {
    setCounter(counter - 1);
  };


  return {
    counter,
    increment,
    descrement,
  };
}; */
